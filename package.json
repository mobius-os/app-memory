{
  "name": "app-memory",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "description": "Dev/test harness only — Mobius installs index.jsx as a single compiled module.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && bash -n fetch.sh && python3 -c \"import py_compile; py_compile.compile('memory_runner.py', cfile='/tmp/app-memory-runner.pyc', doraise=True)\""
  }
}
